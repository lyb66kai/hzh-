# 海智航原型运行环境

这是为 `code_artifact (1).tsx` 配置的 React + TypeScript + Vite + Tailwind 运行环境。

## 运行

```bash
npm install
npm run dev
```

启动后访问终端中显示的本地地址，通常是 `http://localhost:5173`。

## 说明

原始 TSX 文件存在中文乱码和 JSX 标签/引号损坏的问题，`src/App.tsx` 已按原页面结构修复为可编译版本。
